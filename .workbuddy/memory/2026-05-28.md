# 2026-05-28 工作日志

## 画龙点睛成语故事视频生成
- 使用 story-video-producer skill，完整流水线生成成语故事视频
- **视频 API 额度用尽**（429 daily limit 10/10），成功降级到 ImageGen + FFmpeg Ken Burns 方案
- 生成 5 张中国风水墨画风格场景图（1280x720），转为 1920x1080 Ken Burns zoom 视频片段
- edge-tts 配音使用 zh-CN-YunxiaNeural（可爱男声），+8% rate +5Hz pitch，附带 VTT 字幕
- 下载并配置 FFmpeg BtbN build 到 `~/.workbuddy/binaries/ffmpeg/`
- 安装 edge-tts 7.2.8 到 venv
- 最终输出：画龙点睛_成语故事.mp4（26MB, 62s, 1080p）
- 合成流程：标题卡(4s) → 5场景(总计58s) → 合并视频 → 叠加配音+字幕
- **踩坑**：concat 时流格式不一致导致只输出4秒，修复为先统一生成 video-only .ts 再叠加音频字幕
- 将 story-video-producer skill 从 user-level (~/.workbuddy/skills/) 迁移到 project-level (workspace/.workbuddy/skills/)，换设备可随项目文件同步
